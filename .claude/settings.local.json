{
  "permissions": {
    "allow": [
      "Bash(python check_cells.py)",
      "Bash(python -c \"import nbformat; nb = nbformat.read(''evaluate_model.ipynb'', as_version=4); print(f''Total cells: {len(nb.cells)}''); executed = sum(1 for cell in nb.cells if cell.cell_type == ''code'' and ''outputs'' in cell and len(cell.outputs) > 0); code_cells = sum(1 for cell in nb.cells if cell.cell_type == ''code''); print(f''Code cells: {code_cells}''); print(f''Executed cells with output: {executed}''); print(f''All code cells executed: {executed >= code_cells - 1}'')\")",
      "Bash(del test_cell16.py run_notebook.py check_cells.py)"
    ],
    "deny": [],
    "ask": []
  }
}
